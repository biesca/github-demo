# HTTP Server
server:
  port: ${PORT:4449}   # HTTP port

# Discovery Server Access
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:1111/eureka/

logging:
  file: logs/${spring.application.name}.log
  level:
    com.netflix.discovery: 'OFF'
    org.springframework.cloud: 'INFO'
    org.springframework.web: 'INFO'
    es.indra: 'DEBUG'
    
#logging:
#  file: D:/Desarrollo/microservices-config/logs/${spring.application.name}.log
#  level:
#    com.netflix.discovery: 'OFF'
#    org.springframework.cloud: 'INFO'
#    org.springframework.web: 'INFO'
#    es.indra: 'DEBUG'    
